fun main () {                                           //+ sum adds up
    var x = 18 // sum variables                         //- substracts
    var y = 6                                           //* multiply
    var result = x+y                                    // / divides
    println(result)                                     // if divide by integer it will only retun integers

    var result2 = 18+6 // sum in the variable
    println(result2)

    println(18+6) // sum in the prints

    println("the result of x + y is ${x+y}")            // to print operations
    x+=13                                               //add 13 to the core value of 10
    y-=4                                                // substract 4 from the core value
    println("the result of x+y is now ${x+y}")

    //homework set 3 different variables and add them up

    var j = 4
    var k = 5
    var l = 6
    println("j= $j")
    println("j+k =${j+k}")
    println("j+k+l =  ${j+k+l}")
}